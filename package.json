{
  "name": "gem",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "node --test tests/engine/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute gem-db --local --file=./migrations/0001_core.sql && wrangler d1 execute gem-db --local --file=./migrations/0002_routine_logs.sql && wrangler d1 execute gem-db --local --file=./migrations/0003_onboarding.sql && wrangler d1 execute gem-db --local --file=./migrations/0004_workouts.sql",
    "db:migrate:remote": "wrangler d1 execute gem-db --remote --file=./migrations/0001_core.sql && wrangler d1 execute gem-db --remote --file=./migrations/0002_routine_logs.sql && wrangler d1 execute gem-db --remote --file=./migrations/0003_onboarding.sql && wrangler d1 execute gem-db --remote --file=./migrations/0004_workouts.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
