{
  "name": "gem",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "node --test tests/engine/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute gem-db --local --file=./migrations/0001_core.sql && wrangler d1 execute gem-db --local --file=./migrations/0002_routine_logs.sql && wrangler d1 execute gem-db --local --file=./migrations/0003_onboarding.sql",
    "db:migrate:remote": "wrangler d1 execute gem-db --remote --file=./migrations/0001_core.sql && wrangler d1 execute gem-db --remote --file=./migrations/0002_routine_logs.sql && wrangler d1 execute gem-db --remote --file=./migrations/0003_onboarding.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
